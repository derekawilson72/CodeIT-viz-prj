<h1>{{ question.question_text }}</h1> <!--braces contain tags of context data -->
<ul>
  {% for choice in question.choice_set.all %} <!--loop through the choice set -->
  <li>{{ choice.choice_text }}</li>
  {% endfor %}
</ul>
